export type Severity = "critical" | "high" | "medium" | "low" | "info";export type ScannerCategory = "security" | "ai-safety" | "code-quality" | "performance" | "deployment";export type FindingCategory =  | "security"  | "ai-safety"  | "code-quality"  | "performance"  | "deployment"  | "dependencies"  | "testing"  | "documentation"  | "architecture"  | "accessibility";export interface Finding {  id: string;  title: string;  description: string;  severity: Severity;  category: ScannerCategory;  file?: string;  line?: number;  code?: string;  impact: string;  suggestion: string;  references?: string[];  score?: number;  confidence?: number;  effort?: "low" | "medium" | "high";  risk?: "low" | "medium" | "high";}export interface ScannerResult {  scanner: string;  category: ScannerCategory;  findings: Finding[];  durationMs: number;  error?: string;}export interface ProjectDetection {  name: string;  language: string;  frontend?: string;  backend?: string;  packageManager?: string;  database?: string;  orm?: string;  cloudProvider?: string;  deploymentTarget?: string;  ciPlatform?: string;  aiFramework?: string;  containerRuntime?: string;  authProvider?: string;  dependencies: Record<string, string>;  devDependencies: Record<string, string>;  projectRoot: string;}export interface CategoryScore {  category: string;  label: string;  score: number;  severity: "critical" | "high" | "medium" | "low" | "pass";  trend: "up" | "down" | "stable";  findingCount: number;  criticalCount: number;  highCount: number;  mediumCount: number;  lowCount: number;}export interface ScanReport {  id: string;  projectName: string;  projectPath: string;  timestamp: string;  durationMs: number;  detection: ProjectDetection;  scannerResults: ScannerResult[];  categories: CategoryScore[];  overallScore: number;  summary: string;  totalFindings: number;  criticalCount: number;  highCount: number;  mediumCount: number;  lowCount: number;  infoCount: number;}