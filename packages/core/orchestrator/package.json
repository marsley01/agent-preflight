{
  "name": "@agent-preflight/orchestrator",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/",
    "test": "vitest run"
  },
  "dependencies": {
    "@agent-preflight/types": "workspace:*",
    "@agent-preflight/runtime": "workspace:*",
    "@agent-preflight/registry": "workspace:*",
    "@agent-preflight/protocol": "workspace:*",
    "@agent-preflight/memory": "workspace:*",
    "@agent-preflight/utils": "workspace:*",
    "uuid": "^11.1.1"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
