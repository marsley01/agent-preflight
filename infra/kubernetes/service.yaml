apiVersion: v1
kind: Service
metadata:
  name: agent-preflight-service
  namespace: agent-preflight
  labels:
    app.kubernetes.io/name: agent-preflight
    app.kubernetes.io/part-of: agent-preflight
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agent-preflight
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
